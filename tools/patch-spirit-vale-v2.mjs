import fs from 'node:fs';
import path from 'node:path';

const root = path.resolve(process.argv[2] ?? 'source');
const p = (x) => path.join(root, x);
const read = (x) => fs.readFileSync(p(x), 'utf8').replaceAll('\r\n','\n');
const write = (x,s) => { fs.mkdirSync(path.dirname(p(x)),{recursive:true}); fs.writeFileSync(p(x),s,'utf8'); };
function rep(file, re, replacement, label) {
  const src = read(file);
  if (!re.test(src)) throw new Error(`missing anchor ${label} in ${file}`);
  write(file, src.replace(re, replacement));
}
function appendBeforeEnd(file, text) {
  const src = read(file);
  const i = src.lastIndexOf('} as const;');
  if (i < 0) throw new Error(`catalog end missing in ${file}`);
  write(file, src.slice(0,i) + text + '\n' + src.slice(i));
}

// types
rep('packages/combat/src/app-types.ts', /  statType: StatType;\n  status:/, '  statType: StatType;\n  bossOnly: boolean;\n  bossName?: string;\n  status:', 'app state');
rep('packages/combat/src/app-types.ts', /(      setStatType: \{ params: \{ statType: StatType \}; response: DpsAppState \};)/, `$1\n      setBossOnly: { params: { enabled: boolean }; response: DpsAppState };`, 'rpc');

// settings
rep('packages/combat/src/settings.ts', /  statType: StatType;\n  frame:/, '  statType: StatType;\n  bossOnly: boolean;\n  frame:', 'settings type');
rep('packages/combat/src/settings.ts', /  statType: "damage",\n  frame:/, '  statType: "damage",\n  bossOnly: false,\n  frame:', 'settings default');
rep('packages/combat/src/settings.ts', /(      statType: candidate\.statType[^\n]+\n)/, `$1      bossOnly: candidate.bossOnly === true,\n`, 'settings load');

// Live boss meter. Boss = currently dominant monster target by cumulative incoming damage in this session.
rep('packages/combat/src/bun/live-combat-controller.ts', /  private meter: LiveCombatService;\n/, `  private meter: LiveCombatService;\n  private bossMeter: LiveCombatService;\n  private bossTargetId?: number;\n  private bossTargetName?: string;\n  private readonly monsterNames = new Map<number, string>();\n  private readonly targetDamage = new Map<number, number>();\n  private bossBuffer: DpsLogBatch["events"] = [];\n`, 'meter fields');
rep('packages/combat/src/bun/live-combat-controller.ts', /    this\.meter = this\.createMeter\(\);\n    this\.source =/, `    this.meter = this.createMeter();\n    this.bossMeter = this.createMeter();\n    this.source =`, 'constructor');
rep('packages/combat/src/bun/live-combat-controller.ts', /  state\(\): LiveCombatState \{[\s\S]*?\n  \}\n\n  snapshots\(\): LiveCombatState\["snapshots"\] \{[\s\S]*?\n  \}/, `  state(bossOnly = false): LiveCombatState {\n    return {\n      status: this.status,\n      statusDetail: this.statusDetail,\n      personalName: this.personalName,\n      ...(this.meter.getPersonalActorId() === undefined ? {} : { personalActorId: this.meter.getPersonalActorId() }),\n      ...(this.location === undefined ? {} : { location: this.location }),\n      ...(this.logPath === undefined ? {} : { logPath: this.logPath }),\n      snapshots: this.snapshots(bossOnly),\n    };\n  }\n\n  snapshots(bossOnly = false): LiveCombatState["snapshots"] {\n    const record = this.latestRecord();\n    if (!record) return {};\n    const bossRecord = this.latestBossRecord();\n    return {\n      ...(bossOnly ? (bossRecord ? { snapshot: bossRecord.dps } : {}) : { snapshot: record.dps }),\n      tankedSnapshot: record.tps.detail,\n      healSnapshot: record.hps.detail,\n    };\n  }\n\n  bossName(): string | undefined { return this.bossTargetName; }`, 'state/snapshots');
rep('packages/combat/src/bun/live-combat-controller.ts', /    this\.meter\.setPersonalActorId\(actorId\);/, `    this.meter.setPersonalActorId(actorId);\n    this.bossMeter.setPersonalActorId(actorId);`, 'personal actor');
rep('packages/combat/src/bun/live-combat-controller.ts', /    this\.meter\.setPersonalName\(nextName\);/, `    this.meter.setPersonalName(nextName);\n    this.bossMeter.setPersonalName(nextName);`, 'personal name');
rep('packages/combat/src/bun/live-combat-controller.ts', /      this\.meter\.setPersonalActorId\(undefined\);/, `      this.meter.setPersonalActorId(undefined);\n      this.bossMeter.setPersonalActorId(undefined);`, 'clear actor');
rep('packages/combat/src/bun/live-combat-controller.ts', /  reset\(\): void \{\n    this\.meter = this\.createMeter\(\);/, `  reset(): void {\n    this.meter = this.createMeter();\n    this.bossMeter = this.createMeter();\n    this.bossTargetId = undefined;\n    this.bossTargetName = undefined;\n    this.monsterNames.clear();\n    this.targetDamage.clear();\n    this.bossBuffer = [];`, 'reset');
rep('packages/combat/src/bun/live-combat-controller.ts', /    for \(const \{ event, observedAtMs \} of batch\.events\) \{/, `    for (const item of batch.events) {\n      const { event, observedAtMs } = item;`, 'batch loop');
rep('packages/combat/src/bun/live-combat-controller.ts', /(      if \(event\.kind === "actorIdentity"\) this\.meter\.consumeIdentity\(event, observedAtMs\);\n      else this\.meter\.consumeCombat\(event, observedAtMs\);)/, `$1\n      this.bossBuffer.push(item);\n      if (this.bossBuffer.length > 250000) this.bossBuffer.splice(0, 25000);\n      const bossChanged = this.observeBossCandidate(event as any);\n      if (bossChanged) this.rebuildBossMeter();\n      else this.consumeBossEvent(event as any, observedAtMs);`, 'boss consume');
// advance both occurrences
let lc = read('packages/combat/src/bun/live-combat-controller.ts');
lc = lc.replaceAll('if (nowMs !== undefined) this.meter.advance(nowMs);', 'if (nowMs !== undefined) { this.meter.advance(nowMs); this.bossMeter.advance(nowMs); }');
write('packages/combat/src/bun/live-combat-controller.ts', lc);
rep('packages/combat/src/bun/live-combat-controller.ts', /  private latestRecord\(\): CombatEncounterRecord \| undefined \{[\s\S]*?\n  \}\n\n  private updateStatus/, `  private latestRecord(): CombatEncounterRecord | undefined {\n    const state = this.meter.getState(this.relativeNowMs());\n    return state.current ?? state.latestFinished;\n  }\n\n  private latestBossRecord(): CombatEncounterRecord | undefined {\n    const state = this.bossMeter.getState(this.relativeNowMs());\n    return state.current ?? state.latestFinished;\n  }\n\n  private observeBossCandidate(event: any): boolean {\n    if (event.kind === 'monsterIdentity') {\n      if (event.operation === 'reset') this.monsterNames.clear();\n      else if (event.operation === 'remove') this.monsterNames.delete(event.actorId);\n      else if (event.operation === 'upsert') this.monsterNames.set(event.actorId, event.displayName);\n    }\n    if ((event.kind === 'damage' || event.kind === 'death') && event.targetId !== undefined && Number(event.value) > 0) {\n      this.targetDamage.set(event.targetId, (this.targetDamage.get(event.targetId) ?? 0) + Number(event.value));\n    }\n    let bestId; let best = -1;\n    for (const [id, dmg] of this.targetDamage) {\n      if (!this.monsterNames.has(id)) continue;\n      if (dmg > best) { best = dmg; bestId = id; }\n    }\n    if (bestId === undefined || bestId === this.bossTargetId) return false;\n    this.bossTargetId = bestId;\n    this.bossTargetName = this.monsterNames.get(bestId);\n    return true;\n  }\n\n  private consumeBossEvent(event: any, observedAtMs: number): void {\n    if (event.kind === 'actorIdentity') { this.bossMeter.consumeIdentity(event, observedAtMs); return; }\n    if (event.kind === 'damage' || event.kind === 'death') {\n      if (this.bossTargetId !== undefined && event.targetId === this.bossTargetId) this.bossMeter.consumeCombat(event, observedAtMs);\n      return;\n    }\n    this.bossMeter.consumeCombat(event, observedAtMs);\n  }\n\n  private rebuildBossMeter(): void {\n    this.bossMeter = this.createMeter();\n    for (const { event, observedAtMs } of this.bossBuffer) this.consumeBossEvent(event as any, observedAtMs);\n    const nowMs = this.relativeNowMs();\n    if (nowMs !== undefined) this.bossMeter.advance(nowMs);\n  }\n\n  private updateStatus`, 'boss methods');

// controller
rep('packages/combat/src/bun/index.ts', /(      setStatType: \(\{ statType \}\) => \{[\s\S]*?        return appState\(\);\n      \},)/, `$1\n      setBossOnly: ({ enabled }) => {\n        settings.bossOnly = enabled;\n        scheduleSettingsSave();\n        publish();\n        return appState();\n      },`, 'setBossOnly handler');
rep('packages/combat/src/bun/index.ts', /function appState\(\): DpsAppState \{\n  const liveState = live\.state\(\);/, `function appState(): DpsAppState {\n  const liveState = live.state(settings.bossOnly && settings.statType === 'damage');`, 'appState live');
rep('packages/combat/src/bun/index.ts', /(    statType: settings\.statType,\n)/, `$1    bossOnly: settings.bossOnly,\n    ...(live.bossName() === undefined ? {} : { bossName: live.bossName() }),\n`, 'appState fields');
// publish details should use same filtered snapshot
rep('packages/combat/src/bun/index.ts', /    const liveState = live\.state\(\);\n    if \(screen === "live" && liveState\.logPath\) \{/, `    const liveState = live.state(settings.bossOnly && settings.statType === 'damage');\n    if (screen === "live" && liveState.logPath) {`, 'publish live');

// UI
rep('packages/combat/src/mainview/index.tsx', /(function setStatType\(statType: StatType\): void \{[\s\S]*?\n\})/, `$1\n\nfunction setBossOnly(enabled: boolean): void {\n  if (state.value) state.value = { ...state.value, bossOnly: enabled };\n  void desktopView.rpc?.request.setBossOnly({ enabled });\n}`, 'ui setter');
rep('packages/combat/src/mainview/index.tsx', /(<div class="command-bar-actions">\n)/, `$1          {next.statType === "damage" && <label class="boss-only-toggle" title={t("combat.bossOnly.hint")}>\n            <input type="checkbox" checked={next.bossOnly} onChange={(event) => setBossOnly(event.currentTarget.checked)} />\n            <span>{t("combat.bossOnly.label")}</span>\n          </label>}\n          {next.statType === "damage" && next.bossOnly && <span class="zone-pill">{next.bossName ? t("combat.bossOnly.active", { name: next.bossName }) : t("combat.bossOnly.detecting")}</span>}\n`, 'ui toggle');
let css = read('packages/combat/src/mainview/index.css');
css += `\n.boss-only-toggle{display:inline-flex;align-items:center;gap:7px;padding:6px 10px;border:1px solid currentColor;border-radius:8px;cursor:pointer;white-space:nowrap}.boss-only-toggle input{margin:0}\n`;
write('packages/combat/src/mainview/index.css',css);

// Add canonical English keys so MessageKey includes them.
appendBeforeEnd('packages/i18n/locales/en.ts', `  "combat.bossOnly.label": "Boss only",\n  "combat.bossOnly.hint": "Only count damage dealt to the current main boss target.",\n  "combat.bossOnly.detecting": "Detecting boss…",\n  "combat.bossOnly.active": "Boss: {name}",`);

// Traditional Chinese partial catalog. Missing keys fall back to English; key UI areas are covered here.
write('packages/i18n/locales/zh-TW.ts', `import type { PartialMessages } from "../messages.ts";\nexport const zhTW = {\n  "app.name":"Spirit Vale Overlay", "common.ok":"確定", "titleBar.minimize":"最小化", "titleBar.maximize":"最大化", "titleBar.restore":"還原", "titleBar.close":"關閉", "settingsButton.label":"設定",\n  "statType.label":"統計", "statType.aria":"統計類型", "statType.damage":"傷害 (DPS)", "statType.tanked":"承傷 (TPS)", "statType.heal":"治療 (HPS)",\n  "enemyFilter.aria":"依敵人篩選", "enemyFilter.searchPlaceholder":"搜尋敵人", "enemyFilter.clear":"清除篩選", "enemyFilter.all":"所有敵人", "enemyFilter.count.one":"{count} 個敵人", "enemyFilter.count.other":"{count} 個敵人",\n  "combat.window.tag":"DPS", "combat.logSource.label":"戰鬥紀錄來源", "combat.logSource.live":"即時", "combat.logSource.past":"歷史紀錄", "combat.zone.current":"目前地圖：{zone}", "combat.action.deathLog":"死亡紀錄", "combat.action.reset":"重設",\n  "combat.totals.label":"本場總計", "combat.totals.timer":"時間", "combat.totals.encounter":"本場 {metric}", "combat.totals.damage":"總傷害", "combat.totals.damageTaken":"總承傷", "combat.totals.healing":"總治療", "combat.totals.kills":"擊殺數",\n  "combat.tabs.label":"傷害檢視", "combat.tabs.all":"全隊 {metric}", "combat.tabs.personal":"個人", "combat.party.label":"隊伍傷害", "combat.party.rowHint":"雙擊查看玩家即時明細",\n  "combat.column.class":"職業", "combat.column.ign":"角色名稱", "combat.column.kills":"擊殺數", "combat.column.mobsHit":"命中怪物數", "combat.column.critRate":"暴擊率", "combat.column.share":"傷害占比", "combat.column.hits":"命中", "combat.column.crits":"暴擊", "combat.column.critRateLong":"暴擊率", "combat.column.skill":"技能", "combat.column.attackerSkill":"攻擊技能", "combat.column.player":"玩家",\n  "combat.bossOnly.label":"只看 BOSS", "combat.bossOnly.hint":"只統計目前主要 BOSS 受到的傷害；BOSS 會依本場承受總傷害最高的怪物自動辨識。", "combat.bossOnly.detecting":"偵測 BOSS 中…", "combat.bossOnly.active":"BOSS：{name}",\n  "amount.damage":"傷害", "amount.damageTaken":"承受傷害", "amount.healing":"治療",\n  "settings.window.title":"Spirit Vale Overlay 設定", "settings.window.tag":"設定", "settings.nav.label":"設定分類", "settings.search.placeholder":"搜尋設定", "settings.search.label":"搜尋全部設定", "settings.search.results.heading":"搜尋結果",\n  "settings.general.label":"一般", "settings.general.description":"設定程式行為與外觀。", "settings.network.label":"網路", "settings.network.description":"Npcap 封包擷取設定。",\n  "settings.combat.label":"戰鬥", "settings.combat.description":"控制戰鬥追蹤方式。", "settings.combat.resetMeter.label":"切換地圖／頻道時重設計量", "settings.combat.resetMeter.hint":"切換地圖或頻道時建立新場次。", "settings.combat.resetGold.label":"切換地圖／頻道時重設金幣", "settings.combat.pastLogLimit.label":"顯示的歷史場次", "settings.combat.personalDps.label":"個人 DPS 顯示", "settings.combat.personalDps.encounter":"整場平均", "settings.combat.personalDps.live":"即時（近期速率）",\n  "settings.overlay.label":"Overlay", "settings.overlay.description":"設定遊戲內 Overlay 的顯示方式。", "settings.overlay.enabled.label":"啟用 Overlay", "settings.overlay.locked.label":"鎖定 Overlay", "settings.overlay.opacity.label":"透明度", "settings.hotkeys.label":"快捷鍵", "settings.hotkeys.description":"設定鍵盤快捷鍵。", "settings.language.label":"語言", "settings.language.description":"選擇介面語言。",\n  "sessions.title.pastCombatLogs":"歷史戰鬥紀錄", "sessions.title.rewardsReplays":"獎勵重播", "sessions.heading":"最近紀錄", "sessions.refresh":"重新整理", "sessions.loading":"載入最近紀錄…", "sessions.openFolder":"開啟紀錄資料夾", "sessions.open":"開啟", "sessions.active":"進行中",\n  "deathLog.window.tag":"死亡紀錄", "deathLog.heading":"玩家死亡", "deathLog.empty":"此紀錄沒有玩家死亡。", "deathLog.deaths":"死亡", "deathLog.searchPlayer":"搜尋玩家", "deathLog.column.attacker":"攻擊者", "deathLog.column.source":"來源", "deathLog.column.damage":"傷害", "deathLog.column.hits":"命中", "deathLog.column.crits":"暴擊", "deathLog.critical":"暴擊", "deathLog.normal":"一般"\n} satisfies PartialMessages;\n`);
rep('packages/i18n/locale.ts', /import \{ en \} from "\.\/locales\/en\.ts";/, `import { en } from "./locales/en.ts";\nimport { zhTW } from "./locales/zh-TW.ts";`, 'locale import');
rep('packages/i18n/locale.ts', /export const LOCALES = \{ en \}/, `export const LOCALES = { en, "zh-TW": zhTW }`, 'locale list');
rep('packages/i18n/locale.ts', /export const DEFAULT_LOCALE: LocaleCode = "en";/, `export const DEFAULT_LOCALE: LocaleCode = "zh-TW";`, 'locale default');
rep('packages/i18n/translate.ts', /import \{ en \} from "\.\/locales\/en\.ts";/, `import { en } from "./locales/en.ts";\nimport { LOCALES } from "./locale.ts";`, 'translator import');
rep('packages/i18n/translate.ts', /  const catalog: Record<string, string \| undefined> = en;\n  const pluralRules = new Intl\.PluralRules\(DEFAULT_LOCALE\);/, `  const catalog: Record<string, string | undefined> = { ...en, ...(LOCALES[locale] ?? {}) };\n  const pluralRules = new Intl.PluralRules(locale);`, 'translator catalog');

console.log('patched');
